This is a flask api, which is used to create service for creating users and fetching them.
Sqlite is used as the database.
the data are fetched using different data structures like linkedlist, binary search tree, hash map, stack and queue. 